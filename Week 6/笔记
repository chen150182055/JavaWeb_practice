# JSP内置对象application

1. application应用：指的就是我们的web应用（我们创建的web项目）
2. application对象中的数据 是所有用户共享的
3. JSP九大内置对象：out、request、response、session、application、page、pageContexconfig、expection
4. page指的是JSP页面中的属性
5. pageContext 指的是页面上下文环境（page作用域）
6. 作用域：作用范围
7. jsp的四大作用域：pageContext，request，session，application
   --pageContext 当前页面作用域（存放在这里的变量只会在当前页面生效)
   --request 当前请求作用域 （存放在这里的变量只会在当前请求生效）
   --session 当前会话作用域（存放在这里的变量只会在当前会话生效）
   --application 当前应用作用域（存放在这里的变量只会在当前应用生效）
8. Cookie
